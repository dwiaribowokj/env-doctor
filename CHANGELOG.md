# Changelog

## 0.1.0

- Initial MVP CLI.
- Parse `.env` files safely with dotenv.
- Detect duplicate keys, empty values, invalid URL-like values, weak-looking secrets, and missing keys from `.env.example`.
- Avoid printing secret values by default.
